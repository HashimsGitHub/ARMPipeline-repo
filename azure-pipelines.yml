# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'ARMPipeline-conn'
    subscriptionId: '014d52c4-d2a9-494d-9e3f-e78a5c0ab216'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'ARMPipeline-rg'
    location: 'Australia Central'
    templateLocation: 'URL of the file'
    csmFileLink: 'https://raw.githubusercontent.com/HashimsGitHub/ARMPipeline-repo/main/create_web_app/azuredeploy.json?token=GHSAT0AAAAAACQXOBIV2H6GYN64MTJERSROZQU2LWA'
    overrideParameters: '-projectName ARMPipelineProj'
    deploymentMode: 'Incremental'
    deploymentName: 'DeployPipelineTemplate'